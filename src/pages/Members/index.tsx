import { Typography } from '@mui/material';
import Layout from '../../components/Layout';

const Members: React.FC = () => {
   return (
      <Layout>
         <Typography variant='h4'>All Borders</Typography>
      </Layout>
   );
};

export default Members;
